# 云同步改造 — 可执行测试清单

- **被测对象**：`workbench/dispatcher-gh.html`（改造前基线版，KMC 车辆调度单文件 HTML）
- **测试环境**：Chrome（DevTools）+ 一个临时 GitHub 仓库 + Mac mini 自托管 Runner（如有）
- **约定**：每个用例含 `前置条件 / 操作步骤 / 预期结果`；`[阻断]` 标记表示该用例若失败会阻塞后续用例。

---

## A. 单元级检查

### A1. JS 语法检查
- **前置条件**：本机已装 Node.js ≥ 14
- **操作步骤**：
  1. 用 Python 提取内联脚本：`python3 -c "import re;html=open('dispatcher-gh.html',encoding='utf-8').read();m=re.search(r'<script>(.*?)</script>',html,re.S);open('/tmp/kmc.js','w',encoding='utf-8').write(m.group(1))"`
  2. 执行 `node --check /tmp/kmc.js`
  3. 再用浏览器打开页面，Console 无 `SyntaxError` 输出
- **预期结果**：`node --check` 退出码 0；页面加载 Console 零报错。（基线已实测通过）

### A2. 函数定义完整性检查
- **前置条件**：脚本已提取至 `/tmp/kmc.js`
- **操作步骤**：对以下函数逐一执行 `grep -c "function 函数名" /tmp/kmc.js` 或直接看源文件：
  `updateSyncIcon`（应 ≥1）、`manualSync`、`syncAll`、`sbSyncOne`、`sbCreateTables`、`gistRead`、`gistWrite`、`saveGistToken`、`doLogin`、`doSubmit`
- **预期结果**：
  - `updateSyncIcon` 定义位于 L450–468；`let syncStatus='idle'` 位于 L449
  - `sbCreateTables` 定义完整（L444–446，含闭合 `}`）
  - 若改造后新增了 `repoRead/repoWrite/triggerRunner`，三个函数名必须全部出现且各有 1 个 `function`/`const` 定义

### A3. 元素存在性检查
- **前置条件**：浏览器打开页面
- **操作步骤**：Console 执行：
  `['sync-icon','sync-icon-mob','toast','modal-root'].forEach(id=>console.log(id,!!document.getElementById(id)))`
- **预期结果**：4 个元素全部存在（`sync-icon` 在 PC 布局 L246，`sync-icon-mob` 在移动布局 L231）
- **回归提醒**：`saveGistToken` 依赖的 `#st-gist-token` 当前**不存在**（基线即如此）；改造后若新增 token 录入框，需用相同方法验证其 id 与 JS 中 `getElementById` 一致

---

## B. 功能级（端到端，走浏览器 UI）

### B1. 提交需求 → 写 localStorage
- **前置条件**：登录（部门+姓名），Network 面板打开，拦截/断开 GitHub 请求
- **操作步骤**：提交页填日期、出发/返程时间、中英双语文案、目的地、签字 → 点"提交需求"
- **预期结果**：
  - localStorage `kmc2_requests` 新增一条 `status:'pending'` 记录
  - 页面"我的待处理需求"出现该条
  - **注意**：基线中 `doSubmit` 会调用 `sbSyncOne`（L1428），若此前从未成功同步过（`syncAvailable=false`，见 C2），不会发网络请求；若发请求则进入 B2

### B2. 点云朵图标触发 cloudSync → 写 GitHub
- **前置条件**：localStorage `kmc_gist_token` 已设置有效 PAT（仓库 `kmc-sync-data` 写权限）；远端 `kmc-data.json` 已存在；Console 已执行 `syncAvailable=true`（规避 C2 卡死）或此前成功同步过一次
- **操作步骤**：点页面右上角/顶栏云朵图标（`manualSync`）
- **预期结果**：
  - 图标变 🔄（旋转）→ 成功后 ☁️，toast「✅ 已同步 / Synced」
  - Network 出现 `PUT api.github.com/repos/qiuxunshi-png/kmc-sync-data/contents/kmc-data.json`，状态 200，请求体含 `message`、`content`（base64）、`sha`
  - 在 GitHub 网页端打开 `kmc-data.json`，内容与 localStorage 数据一致（中文不乱码）
- **通过标准**：刷新仓库页面能看到提交记录「Sync from KMC Dispatcher」

### B3. 刷新页面从 GitHub 读回
- **前置条件**：B2 已成功写入
- **操作步骤**：清除站点 localStorage → 刷新页面 → 登录
- **预期结果**：Network 出现 `GET raw.githubusercontent.com/.../kmc-data.json`，200；登录后数据（车辆/司机/需求）与 GitHub 一致
- **注意**：基线 `syncAll` 只在远端数据非空时覆盖本地（L512–517），首次读回依赖远端文件已存在

### B4. 多设备同步
- **前置条件**：设备 A 已按 B2 写入；设备 B（或另一浏览器 Profile/隐身窗口）未登录
- **操作步骤**：设备 B 刷新并登录，等待登录后自动同步（L964）；修改一条数据后手动点云朵
- **预期结果**：设备 B 看到设备 A 提交的数据；设备 A 再次手动同步后看到设备 B 的修改；两边 localStorage 最终一致

---

## C. 仓库级（API 直测，用 curl 模拟前端逻辑）

### C1. repoRead（raw 无认证读）
- **前置条件**：仓库 `qiuxunshi-png/kmc-sync-data` 已存在、`main` 分支上有 `kmc-data.json`；**不带 token**
- **操作步骤**：`curl -i "https://raw.githubusercontent.com/qiuxunshi-png/kmc-sync-data/main/kmc-data.json"`
- **预期结果**：HTTP 200，响应体为合法 JSON，含 `vehicles/requests/dispatches/...` 顶层键；若文件不存在则 404（前端此时应把 `gistRead` 的 null 当首次同步处理，见 C4）
- **变体**：用 `?ref=main` 或换 `master` 分支验证分支名；前端硬编码 `/main/`（L400），仓库默认分支若为 `master` 会 404

### C2. repoWrite（Contents API PUT，含 sha 竞态）
- **前置条件**：有写权限 PAT；本地按如下步骤生成 base64：
  `python3 -c "import json,base64;print(base64.b64encode(json.dumps({'k':'v'},ensure_ascii=False).encode('utf-8')).decode())"`（验证中文 UTF-8 编码，前端用 L429 的 `btoa(unescape(encodeURIComponent(...)))`，两者应等价）
- **操作步骤**：
  1. `curl -X GET -H "Authorization: token $PAT" https://api.github.com/repos/qiuxunshi-png/kmc-sync-data/contents/kmc-data.json` → 记下 `sha`
  2. `curl -X PUT -H "Authorization: token $PAT" -H "Accept: application/vnd.github+json" -d '{"message":"Sync test","content":"<base64>","sha":"<sha>"}' https://api.github.com/repos/qiuxunshi-png/kmc-sync-data/contents/kmc-data.json`
- **预期结果**：PUT 返回 200/201；再次 GET 的 `sha` 已变化
- **sha 竞态场景（必测）**：
  1. GET 得到 sha1
  2. 用**另一个 PAT/另一浏览器**先 PUT 成功（sha 变为 sha2）
  3. 再用 sha1 发 PUT → **预期 422 `sha does not match`**，前端 `gistWrite` 应 `return false` 并给出错误提示
  4. **改造验收点**：若实现 `repoWrite` 自动重取 sha 重试，则预期最终 200 且内容为最后一次写入

### C3. triggerRunner（workflow_dispatch）—— ⚠️ 当前未实现，属改造后验收
- **前置条件**：仓库含 Actions workflow（含 `workflow_dispatch` 触发器），PAT 有 `workflow` 权限
- **操作步骤**：
  `curl -X POST -H "Authorization: token $PAT" -H "Accept: application/vnd.github+json" -d '{"ref":"main","inputs":{"action":"read"}}' https://api.github.com/repos/<owner>/<repo>/actions/workflows/<wf-id>/dispatches`
- **预期结果**：HTTP 204；GitHub Actions 页面出现一次 run
- **验收点**：前端新增 `triggerRunner()` 后，手动同步成功（B2）应自动 POST 一次 dispatch；Network 中可见该 POST 且 204

### C4. 首次同步（远端文件不存在）
- **前置条件**：空仓库（无 `kmc-data.json`）
- **操作步骤**：设 token → 点云朵
- **预期结果**：`gistRead` 返回 null → `syncAll` 继续走全量写 → PUT 创建文件成功（sha 为 null 时 PUT 可新建）
- **回归陷阱（基线 bug，改造必须处理）**：此时 `syncAvailable` 仍为 `false`，之后提交需求（B1）不会自动同步。改造后应保证"写成功一次即置 syncAvailable=true"

---

## D. Runner 级（Mac mini 自托管 Actions Runner）

### D1. Actions 页面手动 dispatch（read/write 两种 action）
- **前置条件**：workflow 定义 `inputs.action` 枚举 `read`/`write`；Runner 已注册在线（Settings→Actions→Runners 显示绿色）
- **操作步骤**：Actions 页 → Run workflow → 分别选 `read`、`write` 各执行一次
- **预期结果**：
  - `read`：Runner 拉取 `kmc-data.json` 并落盘到 Mac mini 指定目录；job 日志显示文件内容摘要
  - `write`：Runner 读取本地数据 → commit → push 回仓库；Actions 日志显示 commit hash
  - 失败时日志给出明确错误（token 过期/文件不存在/网络）

### D2. Runner 接收并执行 sync.sh
- **前置条件**：Mac mini 上 `sync.sh` 可执行；`.github/workflows/sync.yml` 中 job 引用该脚本
- **操作步骤**：手动 dispatch `write` → 观察 Runner 执行 `sync.sh` 全程
- **预期结果**：脚本按阶段输出日志（拉取→转换→commit→push）；push 成功 `git log` 出现新提交；脚本对失败分支（无变更、非法 JSON）有明确 `exit` 码与提示

### D3. kmc-data.json 更新后 commit 可见
- **前置条件**：D1/D2 成功
- **操作步骤**：网页端查看仓库 `Commits` 页
- **预期结果**：最新 commit 由 Runner 产生，message 可辨识（如 `sync: update from mac-mini`），文件 diff 只包含数据变更；与前端 `gistWrite` 产生的 commit 共存时无冲突（sha 连续）

---

## E. 异常与降级

### E1. Runner 离线时前端仍可读写
- **前置条件**：Mac mini 关机或 Runner 离线（Actions 页显示灰色）
- **操作步骤**：完成 B2（前端直写 GitHub）+ 手动 dispatch 一次
- **预期结果**：
  - 前端读写 GitHub 不受影响（依赖的是 api.github.com，不依赖 Runner）
  - dispatch 请求仍返回 204，但 workflow 一直处于 `queued`/不执行——**前端不应把"dispatch 已提交"误判为"处理已完成"**；若改造中加了等待 Runner 结果轮询，超时应提示降级

### E2. 网络失败时的错误提示与重试
- **前置条件**：DevTools 开启 Offline，或把 `api.github.com` 请求 block
- **操作步骤**：点云朵同步
- **预期结果**：
  - 图标变 ⚠️，toast「⚠ 同步失败 / Sync failed」（L489）
  - 3 秒后图标回 idle（L492）；60 秒自动同步（L2191）会再次尝试
  - **验收点**：改造后 `manualSync` 失败应有重试按钮/手动重试路径，且错误原因（网络/401/403/422）能区分提示，不能静默吞掉
- **补充**：401/403 表示 token 失效，应提示"重新录入 token"；当前基线仅置 `syncAvailable=false`（L528/L529），Console 有 `syncAll` 错误日志

### E3. payload 非法 JSON 时 sync.sh 的处理
- **前置条件**：向仓库提交一份损坏的 `kmc-data.json`（如 `{bad json`）
- **操作步骤**：dispatch `read` → 观察 Runner 日志；前端刷新 → 登录
- **预期结果**：
  - Runner：`sync.sh` 捕获解析错误并 `exit 1`，日志给出文件名+行号，**不得**用空数据覆盖本地正常数据
  - 前端：`gistRead` 的 `JSON.parse` 抛错被 try/catch 接住返回 null（L398–406），表现为"远端无数据"，不会白屏
  - **验收点**：Runner 端应做"校验失败则跳过写回"的保护，避免坏数据被 commit 覆盖

---

## F. 国内访问可用性验证

### F1. raw.githubusercontent.com 可达性
- **操作步骤**（在目标内网/办公网执行）：`curl -I -m 8 https://raw.githubusercontent.com/qiuxunshi-png/kmc-sync-data/main/kmc-data.json`
- **预期结果**：8 秒内返回 HTTP/2 200；若超时/重定向到认证页，说明被墙或需代理，前端需降级提示"云端读取失败，数据仅存本地"
- **补充**：测 CDN 变体 `raw.githubusercontent.com` 与镜像 `raw.gitmirror.com`（仅做对比，不改代码）

### F2. api.github.com 可达性
- **操作步骤**：`curl -i -m 8 -H "Authorization: token $PAT" https://api.github.com/repos/qiuxunshi-png/kmc-sync-data/contents/kmc-data.json`
- **预期结果**：8 秒内 200；若 401/403 说明 PAT 权限不足（应为 `repo` 或 `public_repo` 写权限）
- **验收点**：内网环境（矿区网络）下应实测上述两条命令，记录 RTT；若 RTT > 5s，前端 10s/8s 超时（L482、L2196）可能误报失败，需调大超时或做指数退避重试

---

## G. 回归清单（改造不得破坏现有功能）

| 用例 | 检查点 |
|---|---|
| G1 | 无 token 时手动同步 → 不抛异常，toast 失败提示（基线路径 L505–507） |
| G2 | 清除全部数据（设置→清空）后 localStorage 重置为默认（L2166） |
| G3 | 60 秒自动同步不与其他操作冲突（L2191 `syncStatus==='syncing'` 防重入） |
| G4 | `updateSyncIcon` 在 PC 与移动两端同时生效，语言切换后状态不丢 |
| G5 | 重命名 `gistRead/gistWrite` 为 `repoRead/repoWrite` 后，grep 确认无残留旧函数调用（调用点清单见现状审查 §3.3） |

---

## 执行顺序建议

`A1→A2→A3 → B1→B2→B3→B4 → C1→C2→C4 → F1→F2 → E2→E3 → D1→D2→D3 → E1`

优先级排序：A/B 是基线回归（必须先全绿再动改造）；C2 的 sha 竞态、C4 的首次同步、E1 的 Runner 降级是改造验收的三个关键风险点，建议作为改造完成门槛。
