# KMC 车辆调度系统 — 云同步改造现状审查

- **审查对象**：`workbench/dispatcher-gh.html`（单文件 HTML，共 2208 行）
- **审查方式**：全量通读（1–2208 行）+ 针对性 grep + `node --check` 语法校验（提取内联 `<script>` 至临时文件，169,239 字符，**校验通过**）
- **审查结论速览**：改造计划中列出的 5 条 bug 中有 3 条**在现代码中不成立**（功能已存在/语法正常）；同步函数本身**已基本完成 Repo 化改造**（GIST_ID 已消失）；但 **Runner / workflow_dispatch 触发链路完全缺失**，且存在若干真实的隐藏问题（token 设置 UI 缺失、首次同步 `syncAvailable` 卡死、仓库名与计划不符）。

---

## 一、改造计划逐项核对

### 1.1 缺失 `updateSyncIcon()` 函数（计划称约 1810–1850 行）—— ❌ 不成立，已存在

**现状：已存在且实现完整**，位置在 **L450–468**，不在 1810–1850 行。

```js
// L449-468
let syncStatus='idle';
function updateSyncIcon(){
  const els=[document.getElementById('sync-icon'),document.getElementById('sync-icon-mob')];
  ...
  const colors={ idle:{...text:'☁️'}, syncing:{...text:'🔄'}, ok:{...text:'☁️'},
                 error:{...text:'⚠️'} };
  const c=colors[syncStatus]||colors.idle;
  el.style.background=c.bg; el.style.borderColor=c.bd; ...
}
```

- 操作元素：`#sync-icon`（PC 侧边栏，HTML 定义于 L246）、`#sync-icon-mob`（移动端顶栏，HTML 定义于 L231），两元素均存在，且都带 `onclick="manualSync()"`。
- 按 `syncStatus` 四态切换（idle/syncing/ok/error），`syncing` 时加旋转动画。逻辑完整，无需"修复"。
- `updateSyncIcon()` 共有 9 处调用点：L481、L491、L492、L497、L964、L2171、L2194、L2198、L2201、L2203（后 4 处在 60 秒自动同步块内）。

### 1.2 应声明全局 `let syncStatus='idle'`（计划称约 1808 行）—— ✅ 已存在

**现状：已存在**，位于 **L449**（不在 1808 行）：

```js
449→let syncStatus='idle';
```

### 1.3 `sbSyncOne()` 函数定义情况（计划称约 410 行）—— ⚠️ 已存在，但有一个逻辑隐患

**现状：已存在**，位于 **L471–474**（不在 410 行）：

```js
// L471-474
async function sbSyncOne(table,data){
  if(!syncAvailable)return;
  await gistWrite({vehicles:DB.vehicles.get(),drivers:DB.drivers.get(),users:DB.users.get(),
    requests:DB.requests.get(),dispatches:DB.dispatches.get(),depts:DB.depts.get()||DEFAULT_DEPTS});
}
```

**存疑点（真实 bug）**：
- L472 引用 `syncAvailable`，但该变量在 **L502** 才声明（`let syncAvailable=false;`）。因 `sbSyncOne` 只会在脚本整体求值后被调用，无 TDZ 运行时错误；但存在**逻辑死锁**：
  - `syncAvailable` 初始为 `false`；
  - 首次同步时若远端 `kmc-data.json` 尚不存在，`gistRead()` 返回 `null`（L401/L404），`syncAll` 不会执行 L511 的 `syncAvailable=true`；
  - 于是之后所有 `sbSyncOne` 调用（提交需求、派车、增删车辆/司机）都会在 L472 静默 `return`，**本地改动不推送到云端**。
- `sbSyncOne` 的 `table`/`data` 参数实际未使用（总是写全量快照），与"单表同步"命名不符——行为上是全量覆盖写。

**调用点（共 4 处，全在本文件内）**：
| 行号 | 场景 |
|---|---|
| L1428 | `doSubmit()` 提交需求后 |
| L1826 | `doDisp()` 合并派车后（连续调用 2 次） |
| L2109 | `addV()` 新增车辆后 |
| L2119 | `addDr()` 新增司机后 |

### 1.4 `sbCreateTables()` 疑似缺闭合 `}`（计划称约 414–418 行）—— ❌ 不成立，语法完整

**现状：已存在且语法完整**，位于 **L444–446**：

```js
444→async function sbCreateTables(){
445→  return gistToken !== '';
446→}
```

**验证**：将整个 `<script>` 块（169,239 字符）抽出运行 `node --check`，**通过**（`NODE_CHECK_OK`）。全文件无 JS 语法错误，不存在缺 `}` 的问题。计划所指行段（414–418）实际是 `gistWrite` 内部（L412–419 为 Contents API GET sha 的请求）。

### 1.5 "约 471–506 行存在 Supabase 残留孤儿代码 `const tables=[...]`"—— ❌ 不成立，未找到

**现状：不存在**。对全文 grep `const tables`、`supabase`（不区分大小写）均**零结果**。计划所指区域（L471–506）实际内容是：
- L471–474 `sbSyncOne`（见 1.3）
- L476 `sbDelOne` 空实现
- L478–493 `manualSync`
- L495–499 `saveGistToken`
- L502–530 `syncAvailable` + `syncAll`

**确有残留物，但内容是"Supabase 空桩"而非 `const tables`**，位于 **L437–440**：

```js
437→async function sbInit(){return null;}
438→async function sbUpsert(t,d){return false;}
439→async function sbLoad(t){return null;}
440→async function sbDel(t,i){return false;}
```

另有**重复声明**：`sbDelOne` 在 L476（空函数）与 L532（调用 `sbDel` 桩）各声明一次，后者覆盖前者；L531 有注释 `//@deprecated_sbDelOne`。`sbDel`/`sbInit`/`sbUpsert`/`sbLoad` 均为空桩，`sbDelOne`（L532）实际调用的 `sbDel` 永远返回 `false`。

---

## 二、同步函数改造核对（GIST_ID → 仓库路径）

### 2.1 GIST_ID → 仓库常量 —— ✅ 已改造，但仓库名与计划不一致

**GIST_ID 已不存在**。L393–396：

```js
393→const REPO_OWNER='qiuxunshi-png';
394→const REPO_NAME='kmc-sync-data';
395→const DATA_FILE='kmc-data.json';
396→let gistToken=localStorage.getItem('kmc_gist_token')||'';
```

⚠️ **差异**：计划指定仓库为 **`qiuxunshi-png/kmc-调度器-同步`**，代码中为 **`qiuxunshi-png/kmc-sync-data`**。需二选一统一，否则读写 404。其余结构（owner + repo + data file）与计划一致。

### 2.2 `gistRead()` → `repoRead()` 语义 —— ✅ 已实现

**L398–406** 已是"raw.githubusercontent.com 无认证读"：

```js
400→const r=await fetch('https://raw.githubusercontent.com/'+REPO_OWNER+'/'+REPO_NAME+'/main/'+DATA_FILE);
401→if(!r.ok)return null;
404→return JSON.parse(text);
```

注意：`gistRead()` 函数名尚未重命名为 `repoRead()`（计划为可选），功能等价；且**读操作也依赖 token**（L396 声明但 L398–406 未使用 token，实际无依赖，OK）。

### 2.3 `gistWrite()` → `repoWrite()` 语义 —— ✅ 已实现

**L408–435** 已是"Contents API GET sha → PUT + base64 + sha"：

```js
412→const getR=await fetch('https://api.github.com/repos/'+REPO_OWNER+'/'+REPO_NAME+'/contents/'+DATA_FILE,{
413→  headers:{'Authorization':'token '+gistToken,'Accept':'application/vnd.github+json'}});
415→let sha=null;
416→if(getR.ok){const j=await getR.json();sha=j.sha;}
420→const r=await fetch('.../contents/'+DATA_FILE,{ method:'PUT', headers:{'Authorization':'token '+gistToken,...},
427→  body:JSON.stringify({ message:'Sync from KMC Dispatcher',
429→    content:btoa(unescape(encodeURIComponent(JSON.stringify(data,null,2)))), sha:sha }) });
433→return r.ok;
```

- **UTF-8 base64 正确处理**（L429 `btoa(unescape(encodeURIComponent(...)))`），中文内容安全。
- **sha 竞态**：GET sha 与 PUT 之间若远端被他人更新，GitHub 返回 422，`gistWrite` 直接 `return false`，**无重试**。改造计划点名的 sha 竞态场景目前无保护。
- 文件不存在时 `getR.ok=false`，`sha=null`，PUT 可创建文件——首次同步路径可行。

### 2.4 `triggerRunner()` / `workflow_dispatch` —— ❌ 完全缺失

对全文 grep `triggerRunner`、`workflow_dispatch`、`workflow`、`sync.sh`、`runner` **均零结果**。**Mac mini 自托管 Runner 的后台处理链路（workflow_dispatch 触发 → sync.sh → commit）在现代码中没有任何实现**，是改造的最大空白。

---

## 三、其他记录

### 3.1 Token 当前怎么获取和存储

- **存储**：localStorage，键 `kmc_gist_token`，**明文**。读取：L396（脚本加载时）、L506（syncAll 内兜底再读一次）。
- **写入路径**：唯一入口 `saveGistToken()`（L495–499），从 `document.getElementById('st-gist-token')` 取值。
  - ⚠️ **`#st-gist-token` 元素在 HTML 中不存在**（全文 grep 仅命中 L496 一处引用，`renderSettings()` L1995–2067 也没有 token 输入框）。`saveGistToken` 一旦被调用会因 `null.value` 抛 TypeError，且**没有任何按钮/UI 调用它**——属于死代码 + 隐性炸弹。
  - **结论**：现页面没有录入 token 的界面，token 只能靠手动在 DevTools 里写 `localStorage`。

### 3.2 同步状态 UI 的现有实现

- 元素：`#sync-icon`（PC，L246）、`#sync-icon-mob`（移动，L231），均 `onclick="manualSync()"`。
- `updateSyncIcon()` L450–468 按 `syncStatus` 四态切换背景/边框/阴影/emoji（☁️/🔄/☁️/⚠️）。
- 触发时机：登录后（L964）、启动恢复会话后（L2171）、手动点图标（L478–493）、**每 60 秒自动同步**（L2191–2204，超时 8s）。
- `manualSync` 超时 10s（L482），`toast` 反馈成功/失败，成功后 3s 回到 idle（L492）。

### 3.3 调用 gistRead / gistWrite 的所有调用点

| 函数 | 定义行 | 调用点 |
|---|---|---|
| `gistRead()` | L398 | L509（`syncAll` 内拉远端） |
| `gistWrite()` | L408 | L473（`sbSyncOne` 内全量写）；L527（`syncAll` 内全量写） |

`syncAll`（L503–530）流程：校验 token → `gistRead()` 拉远端 → 非空则合并进 localStorage（L512–517）→ 组装全量数据 → `gistWrite()` 写回 → 失败置 `syncAvailable=false`（L528）。

---

## 四、审查发现的 3 个最关键现状差距（结论摘要）

1. **Runner 后台处理链路完全缺失**：无 `triggerRunner()`、无 `workflow_dispatch`、无 `sync.sh` 相关代码；前端写完 GitHub 后没有任何机制让 Mac mini runner 消费数据，`kmc-data.json` 更新后也没有 Runner 侧 commit 回写路径。
2. **Token 录入 UI 缺失 + `saveGistToken()` 死代码**：页面无 token 输入界面，`#st-gist-token` 元素不存在，函数一旦调用即抛错；token 明文存 localStorage 且无任何异常/失效处理（403 时仅静默 `syncAvailable=false`）。
3. **首次同步逻辑缺陷**：远端文件不存在（`gistRead` 返回 null）时 `syncAvailable` 保持 `false`，此后所有 `sbSyncOne` 调用在 L472 静默跳过，本地改动不推云端；同时仓库名 `kmc-sync-data` 与计划 `kmc-调度器-同步` 不一致，sha 竞态无重试。

> 备注：计划声称的 3 个 bug（缺 `updateSyncIcon`、缺 `let syncStatus`、`sbCreateTables` 缺 `}`、Supabase `const tables` 孤儿代码）在现代码中**均不成立**——相应功能已存在且语法校验通过。审查基于实测证据，不采信计划中的行号与"缺失"描述。
