# KMC 车辆调度系统 · 增量交付报告（2026-08-16）

> 依据用户确认的 4 项决策，在产物包（MD5 `8acde527`）基础上完成增量开发。
> 团队：software-kmc（工程师寇豆码 + QA 严过关）· 最终 MD5：`69938ef5dbcf8778ec78049e16adb51e`

## TL;DR
开放平台免登录提交 + 截止时间默认 17:00 且管理员可调 + 司机角色完整落地（DVIR 勾选/里程/手写签字/中英责任说明），QA 回归全绿。

---

## 一、交付概览

| 项目 | 状态 |
|---|---|
| 交付状态 | ✅ 全部完成 |
| JS 语法校验（node --check） | ✅ 通过 |
| index.html / dispatcher-gh.html 同步 | ✅ MD5 一致 `69938ef5` |
| QA 功能矩阵 | ✅ 全部通过 |
| 已知问题 | 0 个 P0/P1；2 个 P2 + 1 个 P3 已修复 |

## 二、三项需求落地明细

### ① 开放平台（免登录提交）
| 改动 | 说明 |
|---|---|
| 游客模式 | BOOT 默认进入提交页（`S.user={role:'guest'}`），无登录拦截 |
| 提交人信息 | 提交页 step-sub：部门下拉 + 姓名输入，提交时写入 department/submitter；游客未填则拦截 |
| 身份记住 | `rememberGuest` 存本机身份，"我的待处理/需求池"按 `curName()/curDept()` 过滤 |
| 身份切换 | 保留登录入口（showLogin/closeLogin），管理员连点 7 次 + 密码解锁不变 |

### ② 截止时间 17:00 + 可调
| 改动 | 说明 |
|---|---|
| 默认值 | `getCutoff()` 19:00 → **17:00**，旧 19:00 存量自动迁移 |
| 分钟精度 | 新增 `cutoffMinutes()/afterCutoff()`，按完整 HH:MM 比较（容错 '17:00'/'17'） |
| 设置 UI | 设置页新增「每日截止时间」卡片（time 输入 + 保存），仅管理员可见 |
| 文案联动 | 提交页日期切换、撤回校验、派车空态全部走动态 `afterCutoff()`，无写死 19:00 |

### ③ 司机角色（补全）
| 改动 | 说明 |
|---|---|
| 司机工作台 | 导航新增入口 + 司机选择页（drivers 下拉，重启恢复选中） |
| 任务列表 | `renderDriver` 按日期 + 司机姓名过滤，仅显示本人任务 |
| DVIR 检查 | 8 项勾选**全勾才可提交** + 出发里程必填 + 返程可选 |
| 签字 + 责任 | 手写签名画布（独立 `dSigData`）+ 中英双语责任说明「本人【司机名】确认已完成出车检查…」+ 绿色「确认身份」横幅 |
| 防篡改 | `S.driver` 为空或与派车单司机不符则拦截，driver 字段必须等于当前身份 |
| 云端同步 | 写入 dvir/dvirDate/startKm/endKm/**driverSig/driverConfirmed/driverSigDate** 并 `sbSyncOne` |
| 徽章 | 派车卡/司机卡显示「✅ 司机已确认」 |

### ④ 每日汇总推送
按确认不实现，`wxhook/waphone` 存储键保留。

## 三、SQL 变更（需管理员执行）
`workbench/supabase建表.sql`：dispatches 表新增 3 列（建表段 + 升级段均已含）：
```sql
alter table dispatches add column if not exists "driverSig" text default '';
alter table dispatches add column if not exists "driverConfirmed" boolean default false;
alter table dispatches add column if not exists "driverSigDate" text default '';
```
⚠️ **不执行此升级，司机确认数据无法写入云端。**

## 四、修改文件清单
| 文件 | 变更 |
|---|---|
| `workbench/index.html` | 主版本（4299 行，全部改动） |
| `workbench/dispatcher-gh.html` | 同步副本（MD5 一致） |
| `workbench/supabase建表.sql` | dispatches 补 3 列 |

## 五、遗留 / 注意
1. **云端升级**：登录 Supabase 后台 → SQL Editor 执行升级段补 3 列（一次性）。
2. **管理员密码**：无默认密码，石秋迅需在设置页先设置；管理员功能入口不变。
3. **司机身份**：开放平台设计，司机确认靠姓名锚定 + 签字留痕，未加密码登录（如需可后续加）。
4. **部署**：推送 GitHub Pages 后强制刷新（Cmd+Shift+R）再验证司机/截止功能。
